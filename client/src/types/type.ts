export type User = {
  firstName: string;
  lastName: string;
  email: string;
  password: string;
};

export type RiderProduct = {
  title: string;
  price: number;
  image: string;
};

export type HorseProduct = {
  title: string;
  price: number;
  image: string;
};

export type EditValue = {
  firstName: string;
  lastName: string;
  email: string;
};
